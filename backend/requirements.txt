# Eigenstaendige Web-App – Abhaengigkeiten
Flask>=3.1
Flask-WTF>=1.2
SQLAlchemy>=2.0
openpyxl>=3.1
# Fuer Kuendigung/Ruecknahme: Word-Vorlage fuellen (PDF erzeugt LibreOffice).
# docxtpl ist reines Python und laeuft unveraendert unter Linux und Windows.
python-docx>=1.2
docxtpl>=0.20,<1
# Nur fuer die Desktop-App (Outlook/Word-COM), im Web nicht noetig:
# pywin32>=310
