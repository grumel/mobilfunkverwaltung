# Nur fuer den Server-Betrieb. Zusaetzlich zu requirements.txt.
# Linux bleibt bei gunicorn; Windows verwendet Waitress.
gunicorn>=21.2; platform_system != "Windows"
waitress>=3.0,<4; platform_system == "Windows"
# PostgreSQL-Treiber fuer den Mehrbenutzerbetrieb (siehe deploy/linux/POSTGRES.md).
# Plattformneutral; bei reiner SQLite-Nutzung ungenutzt, aber unschaedlich.
psycopg[binary]>=3.2
