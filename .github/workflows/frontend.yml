name: Frontend

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - chore/monorepo-migration
    paths:
      - "frontend/**"
      - "scripts/frontend-regression-check.mjs"
      - ".github/workflows/frontend.yml"
  pull_request:
    branches:
      - main
    paths:
      - "frontend/**"
      - "scripts/frontend-regression-check.mjs"
      - ".github/workflows/frontend.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Run frontend tests when configured
        working-directory: frontend
        run: npm test --if-present

      - name: Build production frontend
        working-directory: frontend
        run: npm run build

      # Dependency-freier Integritätscheck des Builds: prüft, dass alle im
      # index.html referenzierten /assets-Dateien tatsächlich existieren.
      - name: Verify frontend build integrity
        run: node scripts/frontend-regression-check.mjs
