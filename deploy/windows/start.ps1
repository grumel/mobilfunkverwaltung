# Platzhalter: der produktive Windows-Dienststart wird später implementiert.
# Für lokale Entwicklung kann aus der Repository-Wurzel `python run.py`
# beziehungsweise weiterhin `run_webapp.bat` verwendet werden.

throw "Produktiver Windows-Start ist noch nicht implementiert. Siehe deploy/windows/README.md."
